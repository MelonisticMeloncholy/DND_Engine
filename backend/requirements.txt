# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1
numpy<2.0.0

# ── Cloud LLM — Gemini ────────────────────────────────────────────────────────
# IMPORTANT: Use google-generativeai (the stable SDK), NOT google-genai.
# dm_agent.py was using google.genai (the new experimental SDK) which has
# different import paths and broke streaming. This is the correct package.
google-generativeai==0.7.2

# ── Local LLM — Ollama ────────────────────────────────────────────────────────
ollama==0.2.1

# ── Async Utilities ───────────────────────────────────────────────────────────
anyio==4.4.0
httpx==0.27.0